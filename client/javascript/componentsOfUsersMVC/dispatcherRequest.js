function ControllerOfDispatcherRequests () {
	//1. COMET -> which one?
	//2. 
	this.modelOfDispatcherRequests = new ModelOfDispatcherRequests();
}

function ModelOfDispatcherRequests () {
	//0. constructor = getAllActualRequest()
	//1. getAllActualRequest = function of AJAX (date). return HTML of actual request for all users.
	//2. moveRequest = function of AJAX (idOfRequest, directionOfMoving). return statusOfMoving. if 'Ok' -> moveIt in interface (break accordion. add. create accordion) ? sortAllRequest() ?
	//2. addNewRequest = function (htmlOfRequest). add new request to acceptRequestList.(break accordion. add. create accordion) ? sortAllRequest() ?
	//3. ? sortAllRequest = function (). sort all request ?
	//4. 
}

function ViewOfDispatcherRequest () {
	
}